#include <stdio.h>
#include <stdlib.h>
void gen_finvalue(int num){
int a=0,b=1,c,count,i;

if(num==0){
            return;
        }
        if(num==1){
            printf("%d",a);
            return;
        }
        if(num==1){
            printf("%d",b);
            return;
        }
        for(i=3;i<=num;i++){
       // while(num){
        c=a+b;
    a=b;
    b=c;
    count ++;

    }
    printf("%d",c);


}
int main()
{
    int num;
    scanf("%d",&num);
    gen_finvalue(num);

   // printf("Hello world!\n");
    return 0;
}
