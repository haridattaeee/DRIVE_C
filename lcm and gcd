#include <stdio.h>

int main()
{
  int a,b,lcm,hcf,x,y,r;
  scanf("%d%d",&x,&y);
  
  a=x;
  b=y;
  while(b)
  {
      r=a%b;
      a=b;
      b=r;
  }
  hcf=a;
  lcm=(x*y)/hcf;
  printf("lcm is %d and hcf is %d",lcm,hcf);

    return 0;
}
