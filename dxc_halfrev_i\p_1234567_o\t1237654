/*
#include <stdio.h>
#include <stdlib.h>

int  halfrev(int *arr,int size)
{
    int i,temp;
    for(i=size/2;i<size;i++)

    {
        temp=arr[size-1];
      arr[size-1]=arr[size];
      arr[size]=temp;
      size-=1;
    }



}

int main()
{
    int arr[100],size,i;
    scanf("%d",&size);
    for(i=0;i<size;i++)
    {
        scanf("%d",&arr[i]);
    }
    halfrev(arr,size);
    for(i=0;i<size;i++)
	{
		printf("%d ",arr[i]);
	}

   // printf("Hello world!\n");
    return 0;
}
*/
/*

#include<stdio.h>
void reverseHalfArray(int size,int *inputList)  // 1 2 3 4 5 6 7
{
	int i,temp;
	for(i=size/2;i<size;i++)  //i=size/2
	{
	   // if (i>=size/2)
	   //{
		temp=inputList[size-1];
		inputList[size-1]=inputList[i];
		inputList[i]=temp;
		size-=1;
	    //}
	}
}
int main()
{
	int i,arr[100],len;
	scanf("%d",&len);
	for(i=0;i<len;i++)
	{
		scanf("%d",&arr[i]);
	}
	reverseHalfArray(len,arr);
	for(i=0;i<len;i++)
	{
		printf("%d ",arr[i]);
	}
}

*/
